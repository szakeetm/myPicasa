{
  "name": "my-picasa-workspace",
  "private": true,
  "scripts": {
    "dev": "npm exec --prefix frontend tauri dev -- --config src-tauri/tauri.conf.json",
    "build": "npm exec --prefix frontend tauri build -- --config src-tauri/tauri.conf.json",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build"
  }
}
